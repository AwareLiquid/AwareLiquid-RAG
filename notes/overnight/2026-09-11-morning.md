# 晨报 2026-09-11（M2 类脑推理实验室 · overnight/loop 首轮）

> 随每轮增量更新并 push。本轮：第 1 轮（手动启动；定时任务首触 00:30 将被 `loop.lock` 挡下，属预期行为）。

## R0（本轮）：分支合并修复 + 回归锁 screening

- 方向/参数/seeds：R0 回归锁；命令 `PYTHONHASHSEED=1 .venv/bin/python benchmarks/bench_adapter.py --fake`（单 seed，screening）。
- 实验 commit：`bench:` + `results:` 见本轮 push（`overnight/loop`，SHA 见下）。
- 验收门：
  - ① `publishable()`：不适用（单 seed screening，未作对照结论；protocol 模块自检：`paired_sign_test` 正常返回 p=1.0 判负路径）。
  - ② `scripts/check_results_refs.py`：**PASS**（exit=0）。
  - ③ `scripts/test.sh`（`.venv/bin/python -m pytest -q` 全量离线）：**363 passed**。
- R0 结果：`RESULT: PASS`；输出与 `benchmarks/results/bench_adapter_fake_20260906_r1.log` 逐行一致
  （`diff` IDENTICAL；recall@4 8/8、retention 8/8、1254 vs 22668）。
  归档：`benchmarks/results/r0_bench_fake_20260911_r1.log`。结论：合并后的 bench 行为与旧证据一致，无事故。
  （单 seed screening，不作引用结论，符合 `docs/RESULTS.md` 证据政策。）
- kernel：无（本轮无 Kaggle 任务）。
- Kaggle 配额账本：T4 ~30h/周×双账号，未动用；CPU notebook 未动用；通道状态 UNVERIFIED（CLI 未装、缺凭证）。

## backlog 变化

- 新建 `notes/overnight/backlog.md`（含头注、毕业看板、价值排序、Kaggle 通道登记、PRELIM 定级表）。
- `docs/PREREGISTRATION.md` 追加附录 **R1**（lexical vs hybrid 100 题多种子消融，判优/判负规则先落盘）。
- 毕业看板：CANDIDATE 空 / GRADUATED 空 / REJECTED 空。
- 下一轮方向：**R1 SCREENING 冒烟**——先在本机确认 100 题装配器 + 单 seed 超小子集耗时（≤1h 内）；
  若 e5 下载/全量 >1h 则转 P2（Kaggle 通道验证，需人工给凭证）。

## 需 AricRedemption 人工判定的事项

1. **`feat/evidence-governance` PR 的命运**：本轮已把该分支内容合并进 `overnight/loop`（冲突全部按 `main` 侧解决，
   仅保留治理新增文件 + bench 适配修补）。该 PR 本身循环无权合并/关闭——请决定是合并它、关闭它，
   还是保持原样（`overnight/loop` 最终毕业 PR 会覆盖其内容）。
2. **Kaggle 凭证**：R1 全量若超 1h 需要 Kaggle 通道，届时需要 `~/.kaggle/kaggle.json`（双账号）+ 是否现在就装 CLI 的确认。
3. **`notes/` 当前未被 gitignore 覆盖**（仅 `docs/analysis/` 被忽略）：backlog/晨报将随 `overnight/loop` push 到远程。
   如希望循环笔记保持本地私有，请说一声，我加 `notes/overnight/` 到 `.gitignore`（但那样晨报 push 要求就冲突，需一并改 prompt）。

---

# 增量：2026-09-16 23:00 档（R1 轮，BLOCKED-HUMAN 收轮）

> 观测：2026-09-12 ~ 09-16 的夜间定时档没有任何执行痕迹（无新 commit/晨报/锁）——疑似 Mac 夜间睡眠导致
> cron 未触发。今晚 23:00 档是五天来首次实际执行。若需要夜间循环可靠运行，需让机器整夜不睡眠（人工设置）。

## R1 轮：lexical vs hybrid 消融 —— 预注册冒烟发现硬阻塞，按协议停止等人工

- 方向/参数：R1 SCREENING 冒烟（探针级，不涉对照结论，无新预注册）。
- 命令/证据：`AWARELIQUID_LLM_BACKEND=mock AWARELIQUID_TEST_MODE=1 python evals/run_eval.py`（2.3s/48 题）
  → `benchmarks/results/r1_harness_mock_20260916_r1.log`（两段冒烟）。
- 实验 commit：本轮 `bench:`（run_eval.py 开关）+ `loop:`（notes）见 push。
- 三门验收：
  - ① `publishable()`：不适用（本轮无对照实验运行——正是发现问题所在）。
  - ② `scripts/check_results_refs.py`：**PASS**（exit=0）。
  - ③ pytest 全量离线：**363 passed**（含 run_eval 开关改动后回归）。
- Kaggle/kernel：未动用；配额账本不变（T4 0h/30h×2，CPU 0h；通道仍 UNVERIFIED）。

## 发现（全部留痕，细节见 backlog P0）

1. `data/questions/group_a/` 100/100 题无 `answer` 字段 → R1 预注册指标在该题集不可计算。
2. `evals/questions.jsonl` 48 题**全标注**（5 域，corpus 现成）→ 现成替代题集。
3. `MockChatClient` 只回第一选项（代码确认）→ 离线 accuracy 对检索质量不敏感（mock 27.1% = echo 基线）。
4. 真实模型跑 48 题 ≈ 16.6 万 token（mock 口径估算）；`.env` 有 key，未动。
5. 工程清偿：`evals/run_eval.py` 增 `--retrieval-backend {lexical,hybrid}`（R1 两臂入口，冒烟验证）；
   hybrid 臂前提 = e5 首次下载 ~500MB（HF 可达、transformers 已装）。

## backlog 变化

- R1：SCREENING → **BLOCKED-HUMAN**；P0' 新方向浮现（自适应停止合成冒烟，下轮默认，自带金标全离线）；
  P0（R0 回归锁）标记已闭环。毕业看板无变动。

## 需 AricRedemption 人工判定的事项（累计）

1. **R1 题集裁定**：换 `evals/` 48 题标注集（推荐，即刻可跑真实对比；定位是管线基线）
   vs 造 100 题金标（长期更好，需标注投入）。批准后下轮改预注册附录再跑。
2. **计费授权**：允许用 `.env` 的真实 Qwen key 跑 evals 吗（每轮 ≈16.6 万 token）？
3. **`feat/evidence-governance` PR 去留**（09-11 遗留）：其内容已并入 `overnight/loop`，PR 本身循环无权动。
4. **夜间可靠性**：Mac 夜间睡眠会让定时任务整夜空转——是否设置" prevents sleep"由你定。
5. **`notes/` 随分支 push 到远程**是否接受（09-11 遗留；当前已按 push 处理）。

---

# 增量：2026-09-16 23:30 档（R2 轮：自适应停止 parity 冒烟 NO-GO）

## R2 轮：P0' 自适应停止合成任务 screening #1 —— NO-GO（判负留痕，按预注册处置）

- 方向/参数：parity（train L∈[8,32]，OOD [40,64]）；d=96、K=8、β=0.05、Geometric(0.25)、
  AdamW 1e-3、batch 64、1200 步、CPU 确定性；seed=1。
- 命令：`.venv/bin/python research/exp_halting_parity.py --arm {fixed,adaptive} --seed 1 --steps 1200 --out ...`
- 实验 commit：`1a78fa4`（prereg:）→ 结果 commit 见 push（results: / loop:）。
- 判定（预注册 G1–G4 逐条）：G1 PASS（两次 fixed 运行科学指标逐比特一致；仅 wall_s 22.6/22.7s
  仪表字段差异，已如实标注）；**G2 FAIL**（fixed 0.494 / adaptive 0.508 < 0.55）；**G3 FAIL**
  （mean_steps 1.010 < 1.3，坍缩）；G4 PASS（≤10min/臂，多种子可留本机）。
  → **NO-GO**：按预注册 G2 条款判"任务/容量欠配"，下轮调容量/任务形式重筛，无需人工。
- 三门验收：① publishable 不适用（screening 单 seed）；② check_results_refs **PASS**；
  ③ pytest **363 passed**。
- Kaggle/kernel：未动用；配额账本不变（T4 0h/30h×2；通道 UNVERIFIED，R2 多种子也无需 Kaggle）。
- 过程留痕：首轮实现有 per-position 读出/halt 的池化 bug（运行前未产出任何指标），
  修复为 per-example masked-mean；失败尝试日志另存 `*_attempt1_poolingbug.log`。

## backlog 变化

- P0'：IDEA → **SCREENING NO-GO ×1**，已写明下轮重筛方案（steps 6k–10k / 2 层 trunk /
  长度课程 / 迭代友好任务形式 / halt warm-up，任选组合，均在 G2 处置条款内）。
- 毕业看板无变动（CANDIDATE / GRADUATED / REJECTED 均空——screening 判负不入 REJECTED，
  REJECTED 留给多种子阶段过不了门的最终判定）。

## 需 AricRedemption 人工判定的事项（不变，无新增）

R1 题集裁定、计费授权、evidence-governance PR 去留、夜间防睡眠设置、notes/ 是否私有化——
均见 23:00 档晨报增量；本轮 R2 全流程未新增人工依赖项。

---

# 增量：2026-09-17 00:00 档（R2 screening #2：NO-GO ×2，下轮换任务形式）

## R2 screening #2 —— 容量校准无效，按升级条款转任务形式更换

- 方向/参数：同 R2，配置按 00:03 落盘记录（6000 步 + 长度课程 + halt bias -2；判据 G1–G4 不变）。
- 实验 commit：`prereg:`（配置落盘）见 push；结果 `benchmarks/results/r2_halting_smoke_20260917_r2.{log,*_s1.json}`。
- 判定：G1 PASS；**G2 FAIL**（fixed 0.512/0.482、adaptive 0.488/0.502，loss 卡 ln2——6k 步
  仍无学习信号）；**G3 FAIL**（mean_steps 1.003，bias 只延迟坍缩）；G4 PASS（fixed 89s /
  adaptive 348s，2臂×5seeds ≈ 37 min 仍可本机）。
- 三门：publishable 不适用；check_results_refs PASS；pytest 363 passed。
- **结论：容量不是瓶颈，任务形式是。** 下轮 screening #3 换"查询位置前缀 parity"
  （迭代步有明确前缀扩展增益），配置先落盘再跑。
- 过程事故（自查自纠，留痕）：00:03 预注册 commit 所带脚本 def 行被我的编辑操作损坏，
  三次运行 IndentationError 未产出指标；且轮内我把损坏日志误改名为 attemptA 导致其内容
  被 attempt-B 覆盖（损坏日志仅有 traceback，git 历史中损坏脚本可考）。已引入两道防线：
  运行前 `py_compile` 强制 + 运行器 JSON 存在性守卫（缺文件绝不出 G1 PASS）。
- Kaggle/kernel：未动用，账本不变。

## backlog 变化

- P0'：SCREENING NO-GO ×1 → **×2**，下轮动作明确为任务形式更换（screening #3）。
- 毕业看板无变动；人工判定事项无新增（仍是 23:00/23:30 档晨报里的五条）。

---

# 增量：2026-09-17 00:30 档（R2 screening #3：G2 首次 PASS，NO-GO ×3，#4=终局步）

## R2 screening #3 —— prefix-parity 任务学会了两臂，但 halt 头仍拒绝多想

- 方向/参数：task=prefix_parity（00:35 落盘配置）；6000 步、长度课程、halt bias -2，
  其余同 #2；判据 G1–G4 不变。
- 命令：`research/exp_halting_parity.py --task prefix_parity --arm {fixed,adaptive} --seed 1 --steps 6000`
- 实验 commit：`e584c9d`（prereg:）→ results/loop commit 见 push。
- 判定：G1 PASS；**G2 PASS（三次 screening 来首次）**——fixed 0.666 / adaptive 0.666
  in-dist（loss 0.480，短前缀桶 0.83-0.87），任务形式更换成功；**G3 FAIL**——
  mean_steps 1.028，任务学会了但 halt 仍判一步足够；G4 PASS（92s / 363s）。
  → 综合 NO-GO ×3。
- 机理判读（假设）：读出各步独立池化、step-1 已不差 → 迭代无净收益 → 早停理性。
  分桶：fixed 长前缀桶 0.324 vs adaptive 0.588，但 adaptive 该桶停步也 ≈1.01，
  优势不来自迭代（单 seed，噪声不可排除）。
- 三门：publishable 不适用（screening）；check_results_refs PASS；pytest 363 passed。
- Kaggle/kernel：未动用，账本不变。
- 防线升级生效：本轮 py_compile + 150 步 infra selfcheck 先行，正式三连一次通过，
  无返工。

## backlog 变化

- P0'：NO-GO ×2 → **×3**；下轮 screening #4（progressive-reveal：迭代 k 只见前
  ⌈k/K·L⌉ 位，结构上强制单步不足）已在预注册落盘为**阶梯终局步**——#4 G3 再败则
  P0' REJECTED（机制级阴性结果留痕），主轴转 latent-workspace recurrent（固定深度
  循环）；全过则多种子放行。毕业看板无变动；人工判定事项无新增。

---

# 增量：2026-09-17 01:00 档（R2 终局：screening #4 GO → 多种子 REJECTED，P0' 闭环）

> 本档完成了一整段科研闭环：screening #4 → 多种子终判 → RESULTS.md 归档 → 主轴转向。
> 01:30 档因本档持锁正常跳过（预期行为）。

## screening #4（progressive-reveal）：G1–G4 全过，GO

- adaptive 臂重构：trunk 以 step-1 reveal 掩码跑一次，workspace 迭代 k 只见前
  ⌈k/8·L⌉ 位，读出池化仅覆盖已 reveal 前缀；fixed 臂保持全量单步。
- 判定：G1 PASS；G2 PASS（fixed 0.666 / adaptive 0.619）；**G3 PASS——mean_steps
  2.005，三次 screening 的坍缩终于被打破**（结构强制迭代生效）；G4 PASS（87s/325s）。
- 证据：`benchmarks/results/r2_halting_prefix_20260917_r4.log`；commit `d928f23`/`b4ddb44`。

## 多种子终判（5 seeds × 双臂）：REJECTED

- OOD acc：adaptive [0.547, 0.551, 0.502, 0.555, 0.502] vs fixed [0.553, 0.572, 0.514,
  0.566, 0.537]——**0/5 胜**，配对符号检验 p=0.0625；in-dist 1/5 胜（p=0.375）；
  双臂非双峰；`publishable()` 判负。
- mean_steps 各种子 ≈2.00：halt 学到的是固定 2 步预算，非逐例自适应。
- **机制级阴性结论**：本规模/读出结构下，可学习停步无净收益——即使结构强制迭代
  （progressive reveal），halt 头收敛到的仍是策略常量。已按证据政策追加
  `docs/RESULTS.md` 筛查区 REJECTED 行 + 对账日志 2026-09-17 节（refs PASS）。
- 三门：publishable **False**（判负即本轮主结果）；check_results_refs **PASS**；
  pytest **363 passed**。

## backlog 变化

- **P0' → REJECTED**（毕业看板 REJECTED 首次非空）；可复用资产注明（实验骨架）。
- **P0'' 浮现**：latent workspace recurrent（固定深度循环，Geiping 式）——下轮写新
  预注册（depth dose-response 单调性判据），复用 P0' 任务与验收骨架。

## 需 AricRedemption 人工判定的事项（不变）

仍是五条：R1 题集裁定、计费授权、evidence-governance PR 去留、夜间防睡眠、notes 私有化。
P0' 的 REJECTED 是预注册规则自动判定，无需人工。

---

# 增量：2026-09-17 02:30 档（R3：latent recurrent depth REJECTED——同夜第二个机制阴性结果）

## R3 固定深度循环 dose-response —— 判负，但判得很有信息量

- 方向/配置：prefix_parity，workspace 块循环 k∈{1,2,4,8}（共享权重，trunk 恒 1 次），
  6000 步 + 课程；k∈{1,8}×5 seeds 主门，k∈{2,4}×3 曲线诊断；约 53 分钟本机。
- 实验 commit：`1d25b81`（prereg:）→ results/docs/loop commit 见 push。
- 判定：主门 `publishable(k8_ood, k1_ood)` **判负**——k8 1/5 胜（p=0.375）；
  OOD 曲线平坦（0.548/0.548/0.551/0.534），in-dist k=2 见顶（0.655→0.695→0.697→0.676）。
  G2 各臂过；G4 过（wall 81-351s）；**G1 跨轮确定性 PASS——k=1 seed1 逐比特复现
  R2 fixed_s1 档案**（depth 重构未扰动基线，架构等价性有机器证明）。
- 机制结论（筛查区留痕）：深度收益限于训练分布内拟合，不买长度外推。
- **轴级判读（写入 backlog）**：R2（停步）+ R3（深度）同夜双阴性 → pooled-readout
  小模型 + 合成 prefix-parity 对类脑"测试时计算"机制不构成有效压力测试场；
  后续类脑方向需升级任务难度或模型规模，或转到真实基准。
- 三门：publishable False（阴性即主结果）；check_results_refs PASS；pytest 363 passed。
- Kaggle/kernel：未动用。

## backlog 变化

- P0'' → **REJECTED**（看板第二条）；**P4 浮现**：下一方向定级——升级合成任务难度 /
  模型规模 vs 解阻塞 R1 真实基准（48 题标注集），后者涉及已登记的人工裁定项。
- 毕业看板 REJECTED 两条：P0'（0/5，p=0.0625）、P0''（1/5，p=0.375）。

## 需 AricRedemption 人工判定的事项（不变，无新增）

仍为五条（见 23:00 档晨报）。下轮 P4 定级若选"解阻塞真实基准"，会再次需要
R1 题集/计费两项裁定。
